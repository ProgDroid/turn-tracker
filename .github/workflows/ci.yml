name: CI
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: Format
        run: cargo fmt --all -- --check
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: Test
        run: cargo test --workspace

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      # npm install (not npm ci): the committed lockfile is generated on Windows,
      # whose native binary deps (rolldown/lightningcss/@emnapi) diverge from
      # Linux; a strict `npm ci` rejects it. `npm install` heals the drift.
      - run: npm install --no-audit --no-fund
      - run: npm run build
      - run: npm run test

  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: docker/setup-buildx-action@v4
      - name: Build image (validate Dockerfile, no push)
        uses: docker/build-push-action@v7
        with:
          context: .
          push: false
          tags: turn-tracker:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max
